# dependencies
/node_modules
/.pnp
.pnp.*

# next.js / build output
/.next/
/out/
/build
next-env.d.ts

# production
/dist

# env / secrets
.env
.env*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# os / editor
.DS_Store
*.pem

# vercel
.vercel

# typescript
*.tsbuildinfo

# ---- non-hosting working files (not needed to build/deploy the site) ----
# Claude Code + agent tooling
/.claude/
CLAUDE.md

# internal working docs (voice DNA, SEO/keyword research, design notes, LinkedIn drafts, old-site copy)
/docs/

# asset harvesting scratch (huge originals pulled off the Framer CDN)
/harvest/

# dev-time asset tooling (harvest + optimize scripts) — not needed to build or host
/scripts/
